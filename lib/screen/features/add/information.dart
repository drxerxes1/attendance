import 'package:flutter/material.dart';

class AddInformation extends StatelessWidget {
  const AddInformation({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("Information Content"));
  }
}
