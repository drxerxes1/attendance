import 'package:flutter/material.dart';

class AddVisitors extends StatelessWidget {
  const AddVisitors({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("Visitors Content"));
  }
}
